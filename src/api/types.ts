export type Cat = {
    id: string;
    name: string;
    birthdate: string;
    breed: string;
    location: string;
    gender: string;
    picturePath: string;
}

export type Appointment = {
    appointment: string;
}